name: CI Pipeline


  on:
    push:
      branches:
        - main

 jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: kodu indir
          uses: actions/checkout@v4

        - name: Docker image build al
          run: docker build -t hello-docker .
